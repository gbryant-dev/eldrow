import React, { FC } from "react";
import { StyledHeader, Title } from "./Header.style";

export const Header: FC = () => {
    return (
        <StyledHeader>
            <Title>Eldrow</Title>
        </StyledHeader>
    )
}