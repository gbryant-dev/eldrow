import React, { FC } from "react";
import { Container, Grid } from "./Board.style";

interface Props {

}

export const Board: FC<Props> = ({ }) => {
    return <Container>
        <Grid>Board</Grid>
    </Container>
}